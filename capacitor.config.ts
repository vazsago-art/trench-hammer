import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.trenchhammer.armybuilder',
  appName: 'TH Army Builder',
  webDir: 'dist'
};

export default config;
